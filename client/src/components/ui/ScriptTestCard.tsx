// 🧪 ScriptTestCard.tsx
import React from 'react';
import { Card, CardHeader, CardTitle, CardContent } from '../../card';

const ScriptTestCard = () => (
  <Card>
    <CardHeader>
      <CardTitle>🧪 ScriptTest</CardTitle>
    </CardHeader>
    <CardContent>
      <p>A/B test voice or chat scripts with live stats.</p>
    </CardContent>
  </Card>
);
export default ScriptTestCard;