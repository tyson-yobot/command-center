// 📄 PDFGeneratorCard.tsx
import React from 'react';
import { Card, CardHeader, CardTitle, CardContent } from '../../card';

const PDFGeneratorCard = () => (
  <Card>
    <CardHeader>
      <CardTitle>📄 PDFGenerator</CardTitle>
    </CardHeader>
    <CardContent>
      <p>Generate branded PDF quotes in one click.</p>
    </CardContent>
  </Card>
);
export default PDFGeneratorCard;