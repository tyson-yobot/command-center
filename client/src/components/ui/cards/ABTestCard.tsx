import React from 'react';
import { Card, CardHeader, CardTitle, CardContent } from '../../card';

const ABTestCard = () => (
  <Card>
    <CardHeader>
      <CardTitle>🧾 ABTest</CardTitle>
    </CardHeader>
    <CardContent>
      <p>View results of A/B script tests.</p>
    </CardContent>
  </Card>
);
export default ABTestCard;
