import React from 'react';
import { Card, CardHeader, CardTitle, CardContent } from '../../card';

const SlackAlertsCard = () => (
  <Card>
    <CardHeader>
      <CardTitle>📣 SlackAlerts</CardTitle>
    </CardHeader>
    <CardContent>
      <p>Monitor and send Slack alerts for key events.</p>
    </CardContent>
  </Card>
);
export default SlackAlertsCard;
