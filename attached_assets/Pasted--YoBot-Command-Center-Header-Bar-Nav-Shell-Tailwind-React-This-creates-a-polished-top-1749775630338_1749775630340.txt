// YoBot® Command Center - Header Bar + Nav Shell (Tailwind + React)
// This creates a polished top bar and nav system for client and demo use

import React from 'react';
import { Button } from '@/components/ui/button';
import { DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem } from '@/components/ui/dropdown-menu';
import { User, Bot, Settings, LogOut, ChevronDown, LayoutDashboard, MessageSquare, BookOpen, Mic, FileText, Users } from 'lucide-react';

export default function HeaderBar() {
  return (
    <div className="w-full bg-gradient-to-r from-[#0b1627] to-[#071021] shadow-lg z-50 sticky top-0">
      <div className="flex items-center justify-between px-6 py-4">
        {/* Logo & System Mode */}
        <div className="flex items-center gap-4">
          <div className="text-white text-2xl font-bold flex items-center gap-2">
            <Bot className="text-sky-400" size={28} />
            YoBot<sup>®</sup>
          </div>
          <span className="ml-3 bg-green-600 text-white text-xs px-2 py-1 rounded-md uppercase">Live Mode</span>
        </div>

        {/* Navigation */}
        <div className="flex gap-6 text-sm text-gray-300">
          <div className="hover:text-white cursor-pointer flex items-center gap-1"><LayoutDashboard size={16}/> Dashboard</div>
          <div className="hover:text-white cursor-pointer flex items-center gap-1"><Mic size={16}/> Voice Logs</div>
          <div className="hover:text-white cursor-pointer flex items-center gap-1"><BookOpen size={16}/> RAG + Knowledge</div>
          <div className="hover:text-white cursor-pointer flex items-center gap-1"><MessageSquare size={16}/> QA & Scripts</div>
          <div className="hover:text-white cursor-pointer flex items-center gap-1"><FileText size={16}/> Sales / Orders</div>
          <div className="hover:text-white cursor-pointer flex items-center gap-1"><Users size={16}/> Clients</div>
        </div>

        {/* Profile & Settings */}
        <DropdownMenu>
          <DropdownMenuTrigger className="flex items-center gap-2 text-white hover:text-gray-200">
            <User className="text-white" size={20}/> Daniel Sharpe <ChevronDown size={16}/>
          </DropdownMenuTrigger>
          <DropdownMenuContent className="bg-[#0f172a] border border-gray-700 text-white">
            <DropdownMenuItem className="hover:bg-slate-800"><Settings size={16} className="mr-2"/> Settings</DropdownMenuItem>
            <DropdownMenuItem className="hover:bg-slate-800"><LogOut size={16} className="mr-2"/> Log out</DropdownMenuItem>
          </DropdownMenuContent>
        </DropdownMenu>
      </div>
    </div>
  );
}

// ✅ ADDITIONAL FIXES NEEDED FOR POLISH + MARKET-READINESS:

/*
1. Top Bar Polish (DONE above)
   - Add nav menu, profile info, logout, system badge

2. Add Global Context & System Status
   - Show logged-in user ("Daniel Sharpe")
   - Add "Client: AMT66" tag or dropdown
   - Status ribbon: Bot active/inactive, last deployment time

3. Badge-Based Panels
   - Add badges: "🟢 Active", "🔒 Admin-Only", "🧪 Test-Only", "⚠️ Needs Setup"
   - Use top-right corner badge inside each card

4. Tooltips + Micro-Explanations
   - Add `?` or ℹ icons near technical functions like “Insert into Memory”, “Purge Test Data”, etc.
   - Tooltip shows: "This command sends content to your bot’s long-term RAG memory."

5. Demo Mode Enhancements
   - Inject fake data into panels for demo if no live content
   - Examples: dummy calls, fake confidence scores, sample ROI %
   - Demo toggle switch (Test vs Demo mode)

6. Visual Hierarchy + Group Collapsing
   - Let technical sections (RAG, Memory, Voice Cloning) collapse into accordions
   - Default to "Basic View", toggle to "Developer View"

7. Consistent Empty State UX
   - Don’t leave "--" or "No data" raw
   - Instead show: "📭 Waiting for data...", "🧠 No new training updates"

8. Add “Quick Actions” or “Global Voice Bar”
   - Floating input like "What can I help you automate today?"
   - Acce