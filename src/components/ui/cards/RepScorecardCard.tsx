import React from 'react';
import { Card, CardHeader, CardTitle, CardContent } from '../../card';

const RepScorecardCard = () => (
  <Card>
    <CardHeader>
      <CardTitle>📈 RepScorecard</CardTitle>
    </CardHeader>
    <CardContent>
      <p>Sales rep scorecards and KPIs.</p>
    </CardContent>
  </Card>
);
export default RepScorecardCard;
